# Stagent 真实 LLM 配置模板
# ----------------------------------------------------------------------------
# 用法：复制本文件为 .env.local（已被 .gitignore 忽略，不会提交），填入真实值。
#   cp .env.example .env.local
# headless 脚本（scripts/headless/run.mjs）会自动加载 .env.local；
# 若同名变量已在 shell 中 export，则以 shell 的为准（已存在者优先）。
# 切勿把真实 key 提交或粘贴到聊天里。
# ----------------------------------------------------------------------------

# 必填：DeepSeek API Key（live 模式需要；缺省会报错）
# 也可用 LLM_API_KEY 作为同义回退。
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# 可选：OpenAI 兼容 base URL（默认 https://api.deepseek.com，脚本会自动补 /v1）
LLM_BASE_URL=https://api.deepseek.com

# 可选：默认模型。脚本内置默认是即将退役的 deepseek-chat；
# 这里显式用现行模型：先用便宜的 flash 验证，跑通后可换 deepseek-v4-pro。
LLM_MODEL=deepseek-v4-flash

# 可选：成本优化非对称配置（ADR-0006 推荐默认）
# 叶子 impl/fix/test-run 用 flash；仅 decide 升级到 pro（阶段少、成本可控）。
# LLM_MODEL_DECISION=deepseek-v4-pro
# 可选：main 集成切片或多模块编排不收敛时再开
# LLM_MODEL_INTEGRATION=deepseek-v4-pro
# 可选：异族出题人——仅 test_write 阶段（不再连带 decision/integration）
# LLM_MODEL_TEST_WRITE=deepseek-v4-pro
# LLM_MODEL_TEST_WRITE_BASE_URL=https://api.deepseek.com
# LLM_MODEL_TEST_WRITE_API_KEY=
# 历史别名（test-write 仍接受）：
# LLM_BASE_URL_TEST_WRITE=https://api.deepseek.com
# LLM_API_KEY_TEST_WRITE=

# 可选：成本核算单价（每百万 token，美元），用于 feedback 报告里的花费估算。
# LLM_PRICE_INPUT_PER_MTOK=0.27
# LLM_PRICE_OUTPUT_PER_MTOK=1.10

# 可选：详细日志（逐 phase/backend 打到 stderr）。
# HEADLESS_VERBOSE=1
